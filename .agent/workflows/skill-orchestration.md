---
description: 스킬들을 상황에 맞게 자동 선택하고 오케스트레이션하는 마스터 워크플로우
---

# 스킬 오케스트레이션 워크플로우

> [!IMPORTANT]
> **다중 요청 시 일괄 커밋 필수**
> 모든 수정 완료 → 한 번에 빌드/커밋/푸시

이 워크플로우는 작업 유형에 따라 적절한 스킬을 자동으로 선택합니다.

## 🎯 의사결정 트리

```
[작업 유형 확인]
    │
    ├── 새 기능 개발 → /comprehensive-dev
    │
    ├── 파일 분할/정리 → /safe-refactor
    │
    ├── DB 변경 → /db-migration 스킬
    │
    ├── 버그 수정 → /debugging 스킬
    │
    └── 성능 문제 → /performance 스킬
```

---

## 빠른 진단

### 1. 파일 크기 확인
// turbo
```bash
find src -name "*.tsx" -o -name "*.ts" | xargs wc -l 2>/dev/null | sort -n | tail -5
```

### 2. 빌드 상태
// turbo
```bash
npm run build 2>&1 | tail -5
```

### 3. 타입 에러
// turbo
```bash
npx tsc --noEmit 2>&1 | head -10
```

---

## 스킬 맵

| 상황 | 사용 스킬 | 워크플로우 |
|------|----------|-----------|
| 새 기능 | react-component, db-migration | /comprehensive-dev |
| 리팩토링 | refactoring | /safe-refactor |
| 테스트 추가 | testing | 없음 (스킬만) |
| 배포 | 없음 | /deploy |
| 최적화 | performance | 없음 (스킬만) |
| **AI 기능** | **ai-integration**, supabase | /comprehensive-dev |
| **다중 요청** | **parallel-tasks** | 자동 활성화 |

---

## 조합 패턴

### 패턴 A: 기능 + DB
```
1. /db-migration 스킬로 마이그레이션
2. /supabase 스킬로 Edge Function
3. /react-component로 UI
4. /testing으로 검증
```

### 패턴 B: 리팩토링 + 테스트
```
1. /safe-refactor로 파일 분할
2. /testing으로 회귀 방지
3. /performance로 번들 확인
```

### 패턴 C: 버그 수정
```
1. /debugging으로 원인 분석
2. 수정 후 /testing
3. /deploy로 배포
```

---

## ⚠️ 주의사항

- **단일 변경 원칙**: 한 번에 하나의 변경만
- **검증 필수**: 각 단계 후 `npx tsc --noEmit`
- **롤백 준비**: 커밋 전 `git diff` 확인

---

## 추가 패턴

### 패턴 D: 병렬 기능 개발
```
[다중 요청 수신]
    │
    ├── 1. 의존성 분석 → 병렬 가능 작업 식별
    │
    ├── 2. 각 기능별 파일 수정 (순서 무관)
    │       ├── Feature A (file1, file2)
    │       └── Feature B (file3, file4)
    │
    ├── 3. 전체 빌드 검증
    │       // turbo
    │       npm run build && npx tsc --noEmit
    │
    └── 4. 단일 커밋으로 통합
            git add . && git commit -m "feat: multiple features"
```

### 패턴 E: AI 기능 개발
```
[AI 기능 요청]
    │
    ├── 1. /ai-integration 스킬 → API 설계
    │       ├── Claude/GPT API 선택
    │       ├── 프롬프트 템플릿 작성
    │       └── 스트리밍 응답 설계
    │
    ├── 2. /supabase 스킬 → Edge Function 생성
    │       ├── API 키 환경변수
    │       └── CORS 설정
    │
    ├── 3. /react-component 스킬 → UI 구현
    │       ├── 스트리밍 표시
    │       └── 로딩/에러 상태
    │
    └── 4. /testing 스킬 → 모킹 테스트
            └── API 모킹으로 브라우저 없이 검증
```

### 패턴 F: E2E 테스트 (브라우저 최소화)
```
[테스트 요청]
    │
    ├── 1. 빌드 검증
    │       // turbo
    │       npm run build
    │
    ├── 2. TypeScript 검증
    │       // turbo
    │       npx tsc --noEmit
    │
    ├── 3. Vitest 단위 테스트
    │       // turbo
    │       npm run test
    │
    └── 4. (필요시) 브라우저 테스트
            → 사용자에게 확인 후 진행
```

